{
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "routes": [
    {
      "src": "/api/calcom/webhook",
      "dest": "/api/calcom/webhook.js",
      "methods": ["POST", "OPTIONS"]
    }
  ],
  "buildCommand": "npm run build",
  "outputDirectory": "dist"
}